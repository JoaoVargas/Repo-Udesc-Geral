#include <stdio.h>
#include <math.h>


int main() 
{
    int x, y, i;
    
    scanf("%d %d", x, y);

    printf("Meu pow: %d\nMath pow:%d", pow2(x,y), pow(x,y));

    return 0;
}

int pow2(int x, int y){

}
